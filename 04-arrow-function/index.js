function salutation(myName) {
    return `hello ${myName}`
}

const salutationArrow = (myName) => {
    return `hello ${myName}`
}

console.log(salutation('John doe'))
console.log(salutationArrow('Array john doe'))